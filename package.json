{
  "name": "vscode-lm-api-proxy",
  "displayName": "VS Code LM API Proxy",
  "description": "A VS Code extension that wraps Language Model API as a local HTTP proxy server",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.102.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": ["onStartupFinished"],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "lmApiProxy.start",
        "title": "LM Proxy: Start Server"
      },
      {
        "command": "lmApiProxy.stop",
        "title": "LM Proxy: Stop Server"
      },
      {
        "command": "lmApiProxy.status",
        "title": "LM Proxy: Show Status"
      },
      {
        "command": "lmApiProxy.selectModel",
        "title": "LM Proxy: Select Model"
      },
      {
        "command": "lmApiProxy.setLogLevel",
        "title": "LM Proxy: Set Log Level"
      },
      {
        "command": "lmApiProxy.showOutput",
        "title": "LM Proxy: Show Output Panel"
      },
      {
        "command": "lmApiProxy.clearOutput",
        "title": "LM Proxy: Clear Output Panel"
      }
    ],
    "configuration": {
      "title": "LM API Proxy",
      "properties": {
        "lmApiProxy.port": {
          "type": "number",
          "default": 3000,
          "description": "Port number for the HTTP proxy server"
        },
        "lmApiProxy.autoStart": {
          "type": "boolean",
          "default": false,
          "description": "Automatically start the proxy server when VS Code starts"
        },
        "lmApiProxy.allowCors": {
          "type": "boolean",
          "default": true,
          "description": "Enable CORS support for cross-origin requests"
        },
        "lmApiProxy.logLevel": {
          "type": "number",
          "default": 1,
          "enum": [0, 1, 2, 3],
          "enumDescriptions": [
            "DEBUG - Show all logs including detailed debug information",
            "INFO - Show informational messages and above (default)",
            "WARN - Show warnings and errors only",
            "ERROR - Show errors only"
          ],
          "description": "Log level for the extension output"
        },
        "lmApiProxy.showOutputOnStartup": {
          "type": "boolean",
          "default": false,
          "description": "Automatically show output panel when extension starts"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.102.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.25.3",
    "eslint": "^9.25.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
